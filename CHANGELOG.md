# Changelog

All notable changes to `selexprep` are documented here.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versioning: [SemVer](https://semver.org/).

## [Unreleased]

### Added
- Phase 0 scaffolding: `pyproject.toml` (hatchling backend), MIT license, MkDocs scaffold, GitHub Actions CI (pytest matrix 3.10/3.11/3.12 + ruff + mypy), pre-commit hooks, empty Typer CLI with stub subcommands (`inspect`, `fetch`, `detect`, `extract`, `count`, `qc`, `run`).

## [0.1.0] — TBD

First public release. See implementation plan for the locked v0.1 feature set.
